import { NgModule } from '@angular/core';
import { ScheduleComplaintPage} from './schedule-complaint';
import { IonicPageModule } from 'ionic-angular';

@NgModule({
  declarations: [ScheduleComplaintPage],
  imports: [IonicPageModule.forChild(ScheduleComplaintPage)],
})
export class ScheduleComplaintPageModule { }
